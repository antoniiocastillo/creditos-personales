import { Shell,Top } from '@/components/shell'; import { money } from '@/lib/money';
export default function Reportes(){return <Shell><Top title="Reportes" subtitle="Información de cartera y cobranza"/><div className="kpis">{[['Cartera colocada',money(1842500)],['Recuperado',money(906080)],['Vencido',money(48600)],['Créditos liquidados','43']].map(([l,v])=><div className="card" key={l}><div className="kpi-label">{l}</div><div className="kpi-value">{v}</div></div>)}</div><div className="card"><h2>Reportes disponibles</h2><div className="due"><div className="due-item"><div><strong>Cartera por antigüedad</strong><div className="small">Saldos agrupados por días de atraso</div></div><button className="button">Exportar</button></div><div className="due-item"><div><strong>Pagos por periodo</strong><div className="small">Pagos recibidos por fecha y operador</div></div><button className="button">Exportar</button></div><div className="due-item"><div><strong>Estado de cuenta por cliente</strong><div className="small">Historial de créditos, cargos y pagos</div></div><button className="button">Exportar</button></div></div></div></Shell>}
